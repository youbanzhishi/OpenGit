# OpenGit Server Configuration

# Directory containing bare repositories
repos_dir = "./repos"

# Bind address
bind = "0.0.0.0:9418"

# Policy file
policy_file = "config/policies.yaml"

# Identity file
identity_file = "config/identities.yaml"

# Audit log
audit_file = "data/audit.json"
